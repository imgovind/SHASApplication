@model SHASApplication.Models.configsmartsprinkler

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>configsmartsprinkler</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.DesiredMoisture)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DesiredMoisture)
            @Html.ValidationMessageFor(model => model.DesiredMoisture)
        </div>
        
        <div class="editor-label">
            @Html.LabelFor(model => model.StartTime)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.StartTime)
            @Html.ValidationMessageFor(model => model.StartTime)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
