@model SHASApplication.Models.configsmartplug

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>configsmartplug</legend>

        @Html.HiddenFor(model => model.ID)

        <div class="editor-label">
            @Html.LabelFor(model => model.DeviceID, "device")
        </div>
        <div class="editor-field">
            @Html.DropDownList("DeviceID", String.Empty)
            @Html.ValidationMessageFor(model => model.DeviceID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.StatusID, "configdevicestatu")
        </div>
        <div class="editor-field">
            @Html.DropDownList("StatusID", String.Empty)
            @Html.ValidationMessageFor(model => model.StatusID)
        </div>
        <div class="editor-label">
            @Html.LabelFor(model => model.DeviceThreshold)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DeviceThreshold)
            @Html.ValidationMessageFor(model => model.StatusID)
        </div>
        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

