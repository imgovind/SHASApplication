@model IEnumerable<SHASApplication.Models.configsmartplug>

@{
    ViewBag.Title = "Smart Plug";
}

<h2>Configure Smart Plug</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.device.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.configdevicestatu.Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DeviceThreshold)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.device.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.configdevicestatu.Status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DeviceThreshold)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) <!--|
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })-->
        </td>
    </tr>
}

</table>
